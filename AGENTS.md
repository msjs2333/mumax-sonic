# MuMax-Sonic：代理协作规则

## 范围与入口

本文件适用于整个仓库。先读 README.md；实现按需读取 docs/concept.md、docs/observables.md 和当前阶段计划。用户当次指令优先于本文件。

这是通用微磁可听化工具。不得硬编码 CDW 的目录、易轴、材料、频段或分类阈值。合成声源演示、物理计算、实际设备试听和真实仿真验证必须分别报告。

## 分工与模型偏好

用户已授权在本仓库对可独立任务使用子代理，优先 Luna 和 Terra。每次委派都必须能与主代理的有用本地工作并行；简单修改或强依赖步骤直接完成。

- 主代理：物理定义、契约、任务拆分、共享文件、集成、证据审查和最终交付。
- Terra（当前运行时标识 gpt-5.6-terra）：有明确接口的模块实现、音频/数据流问题、集成审查。
- Luna（当前运行时标识 gpt-5.6-luna）：独立测试、已定义的合成夹具、文档同步、局部检查和清单整理。
- 以上是用户指定的调度偏好，不是模型能力或价格保证。启动时核对实际工具支持；不可静默换成其他模型。
- 默认主代理加两个子代理；第四个并发位置只在有独立工作包时使用。子代理不继续递归拆分，除非主代理明确分配。
- 能指定模型时显式指定。当前工具对完整历史继承有约束，优先用 none/有限上下文并传入完整任务说明；不能只发文件名让子代理猜上下文。

## 每次任务单

包含目标、依赖与冻结接口、允许写入文件、禁止改动范围、适用物理约定、验收方法及报告格式。每个文件同一时间只有一个写入者。

主代理独占项目配置、公共 model.py、包入口、README、阶段状态及 Git 暂存/提交；可显式转交某文件，但须先结束旧写入任务。子代理不得回滚他人改动、擅自扩大接口或自行提交。只读审查可以并行。

子代理报告应简短：完成内容、文件、实际检查及结果、未验证项、需要主代理解决的接口问题。需要升级任务时先给出具体失败证据，不重复生成整套计划。

## 等待与上下文成本

- 优先接收自动完成通知；等待期间主代理继续契约、独立检查或集成准备，不反复询问进度。
- 只有没有独立工作且后续依赖子代理时才调用 wait_agent。当前环境单次阻塞上限为 60 秒时使用 60000 ms，不做 10–30 秒的轮询；若更高层允许更长等待，可使用更长事件等待。
- 超时不等于失败，不自动中断或重复委派。收到消息或等待超时后根据依赖决定下一步；禁止把 list_agents/status 作为高频轮询替代。
- 耗时工作约每 60 秒提供有内容的用户进度；不发送无新信息的循环状态。
- 只共享必要文档、接口和小段输出，不传整个历史、原始场数组或大日志。

## 实现与验证

用户明确要求实时只读链路保持轻量：默认信任生产输出，不增加逐帧内容哈希、历史文件防篡改审计或重复验证。保留解释物理数据和处理半写文件所需的检查；性能优化不以增加审计机制为前提。

遵守 docs/observables.md 的单位、坐标、序参量、连续取向、拓扑符号和有效性规则。注意 ROI 不改物理积分域；缺数据不是零；音频线程不读文件/推进求解器。

测试覆盖科学不变量与用户行为，不镜像实现。文档修改只做相关链接/格式检查。数值测试通过不等于人耳可辨；mock 音频通过不等于 Windows HRTF 已验证。没有运行的检查必须标注未运行。

提交前主代理检查全部差异、相关测试和 git diff --check，只暂存本任务文件。只有已获用户授权的提交/发布才执行；不把本地提交扩大为远程推送。规划阶段不启动 GPU 仿真或改变外部项目。

## 配置含义

AGENTS.md 是仓库协作指令，并不是强制模型路由配置。当前会话已通过子代理工具显式选用 Luna/Terra；未来会话依运行时能力执行此偏好。需要命名代理运行时配置时，另按实际客户端支持核对，不能假定 .agent 文件被自动识别。

官方文件约定：[AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md)；[子代理](https://learn.chatgpt.com/docs/agent-configuration/subagents)。
